[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: testes de stress pesado — 650 clientes, milhares de operações (use: pytest -m slow)
